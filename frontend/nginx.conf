server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /v1/api/ {
        proxy_pass http://backend:8080;
    }
}